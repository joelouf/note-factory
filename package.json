{
  "name": "note-factory",
  "version": "1.0.0",
  "description": "Application to write and save notes using an Express.js backend that retrieves data input from JSON files.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.22.0",
    "uuid": "^9.0.1"
  }
}
